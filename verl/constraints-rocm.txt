# constraints-rocm.txt — freezes packages already present in the current
# ROCm torch environment so `pip install -r requirements.txt -c constraints-rocm.txt`
# can only *add* missing packages, never upgrade/downgrade these.
#
# Regenerate after the env changes:
#   python -c "import importlib.metadata as m; [print(f'{k}=={m.version(k)}') for k in \
#     ['torch','flash-attn','numpy','transformers','accelerate','peft','pyarrow', \
#      'datasets','dill','fastapi','uvicorn','pybind11','packaging','pandas']]"

torch==2.9.1+rocm7.12.0rc0
flash-attn==2.8.3
numpy==2.2.6
transformers==4.57.6
accelerate==1.13.0
peft==0.18.1
pyarrow==23.0.1
datasets==4.8.4
dill==0.4.1
fastapi==0.135.2
uvicorn==0.42.0
pybind11==3.0.2
packaging==26.0
pandas==3.0.1
